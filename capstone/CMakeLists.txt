cmake_minimum_required(VERSION 3.3)
project(capstone)
set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -std=c++11")

#set(HEADER_FILES )

#set(SOURCE_FILES )

#add_library(capstone STATIC ${SOURCE_FILES} ${HEADER_FILES})